import { LargestnumPipe } from './largestnum.pipe';

describe('LargestnumPipe', () => {
  it('create an instance', () => {
    const pipe = new LargestnumPipe();
    expect(pipe).toBeTruthy();
  });
});
