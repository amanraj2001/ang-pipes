import { TwoparamentePipe } from './twoparamente.pipe';

describe('TwoparamentePipe', () => {
  it('create an instance', () => {
    const pipe = new TwoparamentePipe();
    expect(pipe).toBeTruthy();
  });
});
