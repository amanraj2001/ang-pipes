import { LongeststringPipe } from './longeststring.pipe';

describe('LongeststringPipe', () => {
  it('create an instance', () => {
    const pipe = new LongeststringPipe();
    expect(pipe).toBeTruthy();
  });
});
